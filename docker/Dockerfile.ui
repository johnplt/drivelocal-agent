FROM python:3.11-slim

WORKDIR /app

# Installation directe de uv sans image externe
RUN pip install --no-cache-dir uv

COPY pyproject.toml .
RUN uv sync --no-dev

COPY ui/ ./ui

EXPOSE 8501

CMD ["uv", "run", "streamlit", "run", "ui/app.py", "--server.address=0.0.0.0", "--server.port=8501"]