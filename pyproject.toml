[project]
name = "drivelocal-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "johnplt", email = "john.plt@live.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.110.0",
    "pgvector>=0.2.5",
    "psycopg[binary]>=3.1.0",
    "pydantic>=2.6.0",
    "pytest>=8.0.0",
    "sentence-transformers>=2.5.0",
    "sqlalchemy>=2.0.0",
    "streamlit>=1.32.0",
    "uvicorn>=0.28.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]